
**Ultra‑low‑latency A/B multicast market‑data receiver and, price–time order book**  

Built to mirror exchange‑style architectures (e.g., dual redundant UDP multicast, strict sequencing, gap detection, merge, decode, full‑depth book) with pinned threads, zero‑copy steady state, lock‑free queues, snapshots.